apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: fault-vsvc
spec:
  hosts:
  - emailservice
  http:        
  - route:
    - destination:
        host: emailservice 
    fault:
      abort:
        percent: 100
        httpStatus: 500
